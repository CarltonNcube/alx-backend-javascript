// Export type RowID as number
export type RowID = number;

// Define interface RowElement with properties firstName, lastName, and optional age
export interface RowElement {
  firstName: string;
  lastName: string;
  age?: number;
}
